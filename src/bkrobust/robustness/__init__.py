"""Axis Robustness: survival curves under knowledge corruption, and the
`r_val` / statistical-utility Pareto frontier.

Live-tree only. Nothing here may import from the stub scaffold (``graphs``,
``knowledge`` as a package, ``metrics``, ``theory``, ``estimation``, ``data``,
``cfm``, ``representations``, ``utils``) — every function there raises
``NotImplementedError``. Note ``bkrobust.synth.knowledge`` IS live and is the
corruption source; the dead one is top-level ``bkrobust.knowledge``.
"""
