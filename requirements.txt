# Fallback for environments without `uv`/PEP 621 support.
# Prefer:  uv sync   or   pip install -e ".[dev]"
#
# Core (mirrors [project.dependencies] in pyproject.toml)
numpy>=1.26
scipy>=1.11
pandas>=2.1
networkx>=3.2
scikit-learn>=1.4
hydra-core>=1.3
omegaconf>=2.3
matplotlib>=3.8
tqdm>=4.66

# Causal-discovery backend: UNRESOLVED decision point, see pyproject.toml.
# causal-learn>=0.1.3
# gcastle>=1.0.3

# Dev extra
pytest>=8.0
pytest-cov>=5.0
ruff>=0.6
mypy>=1.11
pre-commit>=3.7
jupyter>=1.0
nbstripout>=0.7

# CFM extra -- heavy, install explicitly with `pip install -e ".[cfm]"`
# torch>=2.3
# transformers>=4.44
